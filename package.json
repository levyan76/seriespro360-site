{
  "name": "seriespro360-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx -y serve . -l 3000 --no-clipboard",
    "preview": "npx -y serve . -l 3000 --no-clipboard"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "optionalDependencies": {
    "@esbuild/linux-x64": "^0.28.0",
    "@esbuild/win32-x64": "^0.28.0",
    "@esbuild/darwin-arm64": "^0.28.0"
  }
}
